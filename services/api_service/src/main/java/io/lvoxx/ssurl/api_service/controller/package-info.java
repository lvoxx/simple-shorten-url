/**
 * REST controllers exposing the API service endpoints.
 * <p>
 * Handles authentication, URL management, and user management
 * requests. Delegates all business logic to service layer classes.
 */
package io.lvoxx.ssurl.api_service.controller;
